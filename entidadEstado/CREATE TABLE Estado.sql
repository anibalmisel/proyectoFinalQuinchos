CREATE TABLE Estado (  
    idEstado BIGINT AUTO_INCREMENT PRIMARY KEY, -- idEstado como clave primaria, auto-incremental  
    descripcion VARCHAR(255) NOT NULL -- descripcion, una cadena de texto no nula  
);  